import React from 'react';
import Navbar from '../Components/Navbar';

const Tasks = () => {
    return (
        <div className='task'>
            <Navbar/>
            <h1>Task page</h1>
        </div>

    );
};

export default Tasks;